# Developer Documentation

To start the local development server, use one of the following commands:

- `npx expo start -c` (clears the cache before starting)
- `npx expo start` (starts the server normally)